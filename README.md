# site-template

Blank starter for a new static site. Copy this folder, rename the remote, and go.

```bash
# Quick clone for a new project
cp -r site-template my-new-site
cd my-new-site
git init && git remote add origin <your-repo>
```

Contents: bare-bones `index.html` and `style.css` only.
